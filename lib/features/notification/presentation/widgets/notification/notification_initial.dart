import 'package:flutter/material.dart';

class NotificationInitial extends StatelessWidget {
  const NotificationInitial({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Waiting for notifications..."),
    );
  }
}
